const nameArray = ['Shomi', 'Rumi', 'Promi', 'Joni', 'Moni', 'Honey', 'Koni'];

let arrayLength = nameArray.length;

for (let i = 0; i < arrayLength; i++) {
    let item = nameArray[i];
    //console.log(i, item);
    //console.log(`${item}'s index is ${i}`);
}
console.log(nameArray);