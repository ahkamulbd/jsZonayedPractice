// 5 time series
//let timeSeriesNumber = 5;

// for (let i = 1; i <= 10; i++) {
//     console.log(`${timeSeriesNumber} X ${i} = ${timeSeriesNumber * i}`);
// }

function timeSeriesNumber(number) {
    for (let i = 1; i <= 10; i++) {
        console.log(`${number} X ${i} = ${number * i}`);
    }
}
timeSeriesNumber(5);